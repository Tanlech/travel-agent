"""旅行规划 Agent 系统的应用入口包。

`app` 是当前项目的主包入口，承载整个旅行规划 Agent 系统的核心代码。

从结构上看，这个包当前主要分为几层：
- `contracts`：统一的数据契约、taxonomy 和共享工作区定义
- `engine`：策略层与核心决策逻辑
- `agents`：各类 agent 的协议与默认骨架实现
- `graph`：graph 风格的运行器与状态流转骨架

当前阶段，这个包代表的是“系统内核”，而不是完整的业务规划实现。
"""
