# 工作修改内容

> 按提交时间顺序整理，说明每个提交（commit）具体干了什么。

## 提交时间线总览

| 提交 | 时间 | 说明 |
|---|---|---|
| `2a2d451` | 2026-08-12 22:37 | 初始版本：项目整体搭建 |
| `0fa7a7a` | 2026-08-12 22:55 | 8.12 晚修改：intent 与 prompt/service/schema 修改（未改完） |
| `9ceb3fe` | 2026-08-13 19:58 | 完成 intent 修改，开始改 session |
| `d38e4c5` | 2026-08-13 22:24 | 优化 session 会话模块 |
| `12d7561` | 2026-08-14 17:56 | 完成 context/session/memory/intent 优化：共享类型入 common、产物标记、记忆 Redis 持久化 |
| `f4629a4` | 2026-08-15 19:41 | context/session/memory/intent 再优化：共享模型单一来源、列表追加合并、偏好清洗 |
| `0bd8861` | 2026-08-16 17:10 | domain/infrastructure/observability 当前文件审查：包扁平化、客户端瘦身、容错 |
| `f2099dd` | 2026-08-17 10:48 | weather tool 修改：天气数据源切和风、lodging 规则化、高德 POI 字段补全 |
| `120fdf9` | 2026-08-17 10:48 | weather tool 修改：新增和风天气客户端 qweather_client |
| `6c8e690` | 2026-08-17 16:27 | lodging tool 优化完成：纯规则排序 + 距离/价格综合评分（当前 HEAD） |

---

## 1. `2a2d451` 初始版本（2026-08-12 22:37）

项目首次提交，87 个文件、+10379 行，搭建完整骨架。

### 涉及文件（87 个）

入口/部署：`main.py`、`server.py`、`Dockerfile`、`docker-compose.yml`、`pyproject.toml`、`.env.example`、`.gitignore`、`README.md`、`static/index.html`、`阶段.md`
agents：`orchestrator.py`、`planning.py`、`reflection.py`、`repair.py`、`revise.py`、`schema/{orchestrator,planning,reflection,repair,revise}.py`、`sparse/{orchestrator,planning,reflection,repair,revise}.py`、`prompt/{orchestrator,planning,reflection,repair,revise}.py`
domain：`intent/{schema,service,prompt,adapter}.py`、`session/{schema,service,merge,pipeline,repository,repository_impl,mapper,adapter}.py`、`memory/{manager,schema,store}.py`、`context/{builder,planning,response,session,user}.py`、`intent_type.py`、`stage.py`、`session_context.py`
infrastructure：`llm/{client,schemas}.py`、`amap/client.py`、`config/settings.py`、`redis_client.py`
tools：`attraction.py`、`lodging.py`、`transport.py`、`weather.py`、`schema/{attraction,lodging,transport,weather}.py`、`prompt/{attraction,lodging}.py`
observability：`tracing.py`；budgets：`policy.py`、`tracker.py`；`travel_agent.egg-info/`（构建产物）

### 主要模块

- **agents（规划智能体）**
  - `orchestrator.py`：对话编排入口，`TravelOrchestrator.handle` 按意图/字段状态路由 clarify / planning / revise / qa
  - `planning.py`（1002 行）：`planning_agent.run_pipeline` 完整规划管线——工具观测（景点/天气/住宿）→ 行程聚类 → 骨架生成 → 渲染 LLM draft
  - `reflection.py` / `repair.py` / `revise.py`：规划反思、修复、改稿三类智能体
  - `schema/`：orchestrator / planning / reflection / repair / revise 的入参出参模型
  - `sparse/`：各智能体的轻量版（prompt 直接生成结构）
  - `prompt/`：各智能体的 system prompt
- **domain（领域层）**
  - `intent/`：意图识别模块——schema（IntentType 枚举、IntentPlanningRequest、IntentRecognitionInput/Output）、service（LLM 识别 + 规则 fallback）、prompt、adapter
  - `session/`：会话模块——schema（SessionState、ConversationStage、SessionRequestState 等）、service（状态机 `_decide_stage`）、merge（patch 合并）、pipeline（intent↔session 桥接）、repository（存储接口）+ repository_impl（Redis 实现）、mapper（session↔planning 映射）、adapter
  - `memory/`：用户偏好/行程记忆的 manager / schema / store
  - `context/`：SessionContext / PlanningContext / ResponseContext / UserContext 与 builder
  - `intent_type.py`、`stage.py`、`session_context.py`：共享类型定义
- **infrastructure（基础设施）**
  - `llm/client.py`：LLM 客户端封装（openai + httpx）
  - `amap/client.py`：高德地图工具客户端
  - `redis_client.py`、`config/settings.py`：Redis 连接与配置
- **tools（工具层）**：attraction（景点）、lodging（住宿）、transport（交通）、weather（天气）及其 schema/prompt
- **observability**：`tracing.py`（trace_id 链路追踪；结构化日志 monitoring 尚未落地，见 `9ceb3fe`）
- **budgets**：Token 预算策略与追踪
- **入口与部署**：`main.py`、`server.py`（FastAPI 服务）、`Dockerfile`、`docker-compose.yml`（redis 7-alpine + app）、`pyproject.toml`、`.env.example`、`README.md`、`static/index.html`（前端页面）、`阶段.md`

---

## 2. `0fa7a7a` 8.12 晚修改（2026-08-12 22:55）

只改了 `app/domain/intent/service.py`（+9 / -6），意图识别的规则 fallback 收尾：

### 涉及文件

- `app/domain/intent/service.py`（唯一改动文件）

### 修改内容

1. **fallback 意图优先级调整**
   - 旧：`revise_plan → qa → clarification/new_plan → unknown`
   - 新：`end_session（告别语）→ revise_plan（已有行程+改稿语）→ confirm（确认语）→ qa（问句/闲聊）→ clarification/new_plan（字段判定）→ unknown`
   - `reject` 明确改为依赖 LLM 判定（后续再补规则）
2. **日期提取正则优化**：中文日期对（`8月15到8月17号`）——"到/日"两端均可省略，第二段月份可缺省（沿用第一个），提升口语日期解析命中率
3. **目的地停用词扩充**：新增"要""景点"等常见引导语/语气词，避免污染目的地提取
4. **confirm 判定加否定防护**：原话含"不行/行不行"时不再误判为确认

---

## 3. `9ceb3fe` 完成 intent 修改，开始改 session（2026-08-13 19:58）

23 个文件、+494 / -335，intent 收尾 + session 第一轮重构。

### 3.1 共享类型抽取（消除重复定义与循环依赖）

- 新增 `app/domain/intent_type.py`：`IntentType`（StrEnum）、`RevisionScope`（Literal），从 intent/schema.py 抽出，intent/session 两层共用
- 新增 `app/domain/stage.py`：`ConversationStage`，从 session/schema.py 抽出，session/context/memory 三层共用，消除 context→session 循环依赖
- 新增 `app/domain/session_context.py`：`SessionContextView`（conversation_stage + revision_count），session 投影给 intent 的轻量上下文
- 删除 `session/adapter.py`、`session/repository_impl.py`（实现并入 repository.py，入口统一）

### 3.2 intent 层增强

- `schema.py`：
  - `IntentPlanningRequest` 增加 `days`/`travelers` 正整数校验、日期统一 `normalize_date` 归一化（含跨年）；`None` 表示"未提供"放行，缺字段由 merge 层按需追问
  - `session_context` 字段由 `dict` 收紧为 `SessionContextView`（强类型：stage + revision_count）
  - `IntentRecognitionOutput` 一致性约束加强：非 revise 意图一律清空 `revision_scope_hint` 与 `should_load_existing_artifacts`；qa/confirm 等收尾类意图不承载需求字段
  - `_clean_field_value` 整数解析兼容 `float`（如 3.0，`value.is_integer()` 校验），非整数的 float 拒绝
- `service.py`：
  - `recognize` 增加 LLM / fallback 双路径识别日志（记录 raw → intent → patch → missing → scope）
  - fallback 新增 **reject 判定优先级**（"算了/不用了/不规划了"优先于 revise 与字段判定，避免拒绝被误判为重新规划/追问）
  - 关键词匹配全面从 `any(kw in text)` 重构为**预编译正则**（`_compile_keywords`）：新增 `_REJECT_RE`、`_END_SESSION_RE`、`_CONFIRM_RE`、`_QA_RE`、`_REVISION_STRONG_RE` / `_REVISION_WEAK_RE` / `_REVISION_OBJECT_RE`；`_is_revision_message` 排除"改天/改日"，弱动词需搭配行程对象词
- `adapter.py`：session_context 直接传 `SessionContextView` 对象，不再 dict 展开
- `prompt.py`：LLM prompt 补充 reject 示例（"算了"/"不用了"/"不规划了" → reject），提示 LLM 按语义判意图

### 3.3 session 层重构

- `schema.py`：
  - 移除本地 `ConversationStage` / `SessionIntentType` / `SessionRevisionScope`，改用共享定义（intent_type.py / stage.py / session_context.py）
  - `SessionRequestState` 增加正整数校验 + 日期归一化 + 区间方向修正（复用 intent 层 `normalize_date`，统一口径）
  - 精简无用字段：`SessionArtifactSummary` 删除 has_current_plan / has_current_draft / plan_revision_count / draft_revision_count；`SessionMergeResult` 删除 previous_state / applied_patch / changed_fields；`SessionState` 删除 current_turn_patch / missing_fields_snapshot（这些字段被写入但从未被下游消费，属于无效状态）
  - `recent_messages` 由 `dict` 改为强类型 `ChatMessage`
  - `SessionIntentView.session_context` 由 `dict` 改为 `SessionContextView`
- `service.py`（状态机收紧）：
  - 删除 applied_patch / missing_fields_snapshot 写入与 artifacts 标记逻辑（has_current_plan 等）
  - "new_plan + 字段齐强制 ready_to_plan"兜底**移入** `_decide_stage` 状态机内部，删除 service 层的二次修正
  - `_decide_stage` 简化：unknown 及未显式处理意图一律**保持当前阶段**，删除"按已有字段推断阶段"的兜底分支——避免"字段恰好齐 + 一句闲聊(unknown)"被误判成 ready_to_plan 触发重新规划
- `merge.py`：同步精简——删除 `previous_state` / `applied_patch` / `changed_fields` 的组装与追踪（与 schema.py 删字段对应），只保留 `next_state` + `remaining_missing_fields` 输出
- `pipeline.py`：**删除 session/adapter.py**，`adapt_intent_result_to_session`、`adapt_session_state_to_intent_view` 两个 adapter 函数**内联进 pipeline.py**；`mode="json"` 序列化 StrEnum，避免枚举对象传入 session Literal 字段；intent/session 双向契约在 pipeline 内统一编排
- `repository.py`：从"纯 Protocol 接口"扩展为 **RedisSessionRepository 完整实现**（原 repository_impl.py 实现并入并删除该文件）——新增 `session:{id}:state` / `session:{id}:artifacts` 双 key 结构、TTL 过期、state/artifacts 读写方法、`get_redis()` 懒加载单例
- `mapper.py`：`stage_to_execution_mode` 移除已废弃的 `"clarification"` 阶段（阶段名统一后无此值）
- `context/session.py`：`conversation_stage` 类型 str → `ConversationStage`，默认值 `"new_plan"` → `"collecting_destination"`（阶段名全局统一）
- `memory/`：
  - `schema.py`：`SessionMemory.conversation_stage` 改用 `ConversationStage`，默认改 `collecting_destination`
  - `manager.py`：`family_friendly` / `senior_friendly` 判定从"拼接 travelers 文本匹配"改为"在 preferences 里匹配关键词"（亲子/儿童/带娃/带小孩/family/kids；老人/长辈/父母/银发/senior/elder），语义更贴合用户偏好表述；conversation_stage 默认值同步
- `orchestrator.py`：
  - `PlanningOrchestrator.resolve` 阶段名规范化（对齐新状态机）：`"revise_plan"→"revise_ready"`、`"new_plan"→"ready_to_plan"`、`"clarification"→"collecting_requirements"`
  - planning 分支：删除 `artifacts.has_current_plan/has_current_draft` 标记；规划完成**强制 `conversation_stage = "completed"`**，避免后续闲聊被误判 ready_to_plan
  - revise 分支：改稿完成同样置 `"completed"`（连续改稿仍靠 latest_plan_summary 判断）
  - qa 分支：LLM 自由对话失败时的降级文案从"暴露 intent.reasoning"改为**友好引导语**（"告诉我目的地、游玩日期和人数…"）
  - `_qa_chat_reply`：recent_messages 改用 `model_dump()`（元素已由 dict 改为 ChatMessage）

### 3.4 入口与配套

- `monitoring.py`：结构化日志封装（+39 行）
- `server.py`：import 从已删除的 `repository_impl` 改为 `repository`；`get_session` 的 recent_messages 用 `model_dump()`
- `main.py`：增加 `logging.basicConfig`（INFO 级别、统一格式），便于观察识别日志
- `test/test_intent.py`：意图识别测试大幅扩增（+126 行，覆盖新增 reject/正则/一致性约束）

### 涉及文件（23 个）

新增：`domain/intent_type.py`、`domain/stage.py`、`domain/session_context.py`、`observability/monitoring.py`、`test/test_intent.py`
修改：`agents/orchestrator.py`、`domain/context/session.py`、`domain/intent/{adapter,prompt,schema,service}.py`、`domain/memory/{manager,schema}.py`、`domain/session/{mapper,merge,pipeline,repository,schema,service}.py`、`server.py`、`main.py`
删除：`domain/session/adapter.py`、`domain/session/repository_impl.py`

---

## 4. `d38e4c5` 优化 session 会话模块（2026-08-13 22:24）

16 个文件、+279 / -214。围绕四个方向深度优化 session。

### 4.1 并发安全（核心）

**问题背景**：会话状态存 Redis，此前"读→改→写"三步无任何并发保护。两个并发请求（如同会话的两条消息）会各自读旧状态、分别写回，后写者覆盖先写者的改动，造成状态丢失。规划/改稿分支里 artifacts 与 state 分开写，还会出现"产物是新 plan、摘要却是旧 plan"的错位。

- `session/schema.py`：`SessionState` 新增 `version` 乐观版本号字段（默认 0，每次成功保存 +1）
- `session/repository.py`：
  - `save` 改为**乐观并发写入**：`WATCH state key` → 比较 Redis 中当前版本与会话携带的 version → 一致则 `MULTI` 内写入（version+1 后落库）、`WatchError` 重置重放；不一致返回 `False` 拒绝覆盖，由调用方重新加载重试
  - 新增 `save_with_artifacts`：state 与 plan/draft 放进**同一 `MULTI` 事务**提交，冲突时 state 与 artifacts **整体回滚**（零写入），从根上消除"新 plan + 旧摘要"错位
  - 抽公共核心 `_optimistic_save`：`save` 与 `save_with_artifacts` 共用同一套版本校验逻辑
  - 删除孤儿方法 `save_artifacts`（无条件覆盖写产物的旧入口，全项目零残留引用）
  - 空 `session_id` 防护：key 构建抛异常、save 返回 `False`
  - `load` 时顺带续期 TTL（覆盖只读场景如 GET /session，活跃会话不会过期）
- `orchestrator.py`：
  - 新增 `_resolve_intent` 快路径，单请求内完成：`载入（或 initialize）→ 填充 plan_summary（必要时从 artifacts 生成）→ 构建 user_context → intent 识别 + 状态合并 → 追加用户消息 → 乐观保存`；`save` 返回 `False` 时基于最新状态重算并**重试最多 3 次**，耗尽则告警并放弃落库（响应仍有效）
  - 用户消息移到意图识别**之后**追加到 recent_messages（只含历史、不含本轮），重试天然幂等，不会重复记录
  - planning/revise 分支的最终保存统一走原子 `save_with_artifacts`，冲突只告警不阻塞响应

### 4.2 清理遗留双轨

- `memory/`：删除 `SessionMemory`、`build_session_context`、`persist_session_memory`、`load_session_artifacts` 及 MemoryStore 的 session 存储（纯内存 dict、进程重启即丢的旧会话存储，统一改走 Redis）
- `context/builder.py`：改为纯函数推导只读 `SessionContext`，不再从内存会话记忆拼接
- `orchestrator.py`：`PlanningOrchestrator.resolve` 从"改状态的对话决策"**降级为只读字段校验**——删除对 session 的一切修改（pending_questions / conversation_stage / revision_count / last_destination）与 ready_to_revise 分支、artifacts 读取；判定口径由旧的 `destination + days` 对齐为 `REQUIRED_FIELDS`（destination + start_date + end_date），与状态机单一来源
- `planning.py`：不再写回 `updated_session`，resolve 仅作最后一道防御性校验

### 4.3 健壮性

- 空 `session_id` 防护（key 构建 / save 拒绝）
- `load` 顺带续期 TTL
- `_resolve_intent` 预读取 artifacts 供 revise 分支复用，避免同请求内重复读 Redis
- 删除从未被消费的死契约 `should_load_existing_artifacts`（含 fallback、schema、测试）

### 4.4 状态机正确性

- `session/service.py`：`new_plan` 时重置 `revision_count`，避免历史改稿计数污染新一轮规划
- `collecting_requirements` 死分支加注释说明触发条件（当前 `REQUIRED_FIELDS` 仅 3 字段），保留结构便于未来扩展

### 4.5 配套

- `monitoring.py`：`StructuredLogger` 增加 `warning` 方法（供并发冲突告警使用）
- `test/test_intent.py`：用例适配（15 passed）
- `egg-info`：构建产物自动更新（fastapi/uvicorn 依赖）

### 4.6 验证

- 15 个测试全部通过（agent4travel 环境）
- 全部改动文件 `py_compile` 通过
- 注入假 Redis 验证：版本递增、冲突拒绝、原子提交成功/整体回滚、空 id 防护、artifacts 读写
- 验证状态机 `revision_count` 重置行为与 resolve 只读性

### 涉及文件（16 个）

修改：`agents/{orchestrator,planning}.py`、`domain/context/builder.py`、`domain/intent/{schema,service}.py`、`domain/memory/{manager,schema,store}.py`、`domain/session/{repository,schema,service}.py`、`observability/monitoring.py`、`test/test_intent.py`
构建产物：`travel_agent.egg-info/{PKG-INFO,SOURCES.txt,requires.txt}`（自动更新）

---

## 5. `12d7561` 完成 context/session/memory/intent 的优化（2026-08-14 17:56）

36 个文件、+892 / -1860。在 d38e4c5 打下的会话/记忆基础上全面收紧：共享类型统一迁入 `domain/common/`、意图层引入 `has_plan` 判据、会话层补产物标记与损坏容错、记忆层落地 Redis 持久化。

### 5.1 共享类型迁入 domain/common（消除三层重复定义）

- 新增 `common/chat.py`：`ChatMessage` / `ChatRole`（intent / session 层共用，不再由 intent 层定义）
- 新增 `common/dates.py`：`normalize_date` 从 intent/schema.py 抽出（intent / session 统一合法性 + 跨年口径）
- `intent_type.py`、`stage.py`、`session_context.py` 从 `domain/` 根目录迁入 `common/`，各层 import 同步更新
- README 重写（597 → 162 行）：从旧 demo 文档更新为多 Agent 系统架构说明（核心能力 / 架构总览 / 分层职责 / 运行方式 / 环境变量 / 核心设计）
- 删除 `agents/prompt/orchestrator.py`、`agents/sparse/orchestrator.py`（LLM 路由 prompt——实际路由由代码完成，属死代码）
- `.gitignore` 增加 `*.egg-info/`；删除 egg-info 构建产物与 `阶段.md`

### 5.2 意图层

- `IntentRecognitionInput` 新增 `has_plan`（会话层产物落库标记，比 `latest_plan_summary` 更可靠的"已有行程"判据）
- `_has_existing_plan` 判据升级：`has_plan` > `latest_plan_summary` > `revision_count` > 阶段（revise_ready/revise_collecting/completed/closed）
- prompt 展示 `has_plan`；`normalize_date`、`ChatMessage` 改引 common 定义

### 5.3 会话层

- `SessionRequestState` 改为继承 `IntentPlanningRequest`（消除双份字段定义与校验漂移）
- `SessionArtifactSummary` 新增 `has_plan` / `plan_updated_at` + 不变量校验（`has_plan=True` 必有写入时间、无产物不留摘要）
- `SessionState` 新增 `session_id` / `user_id` 前置校验（空值 schema 层直接拒绝）、`closed_at` 生命周期时间戳、`created_at` / `updated_at` 由 str 改 datetime
- `SessionMergeResult` 新增 `changed_fields`（实际变更字段，审计/回显用；日期归一推导出的变化也计入）
- `merge._normalize_date_range`：patch 显式给 `days` 时以 days 为准重算区间（"改天数"不再被区间权威吞掉）
- 状态机：`confirm` 需 `has_plan` 才收尾 completed；`qa/reject` 仅在对话型阶段写回 qa；缺字段的 `revise_collecting` 不计入改稿次数（仅 `revise_plan` 且字段齐才 +1）
- repository：state / artifacts JSON 损坏容错（按新会话 / 无产物处理，不再整条 500）；`load` 时产物 key 同步续期（防"state 存活、artifacts 过期"）；`_optimistic_save` 加 5 次尝试上限防无限竞争

### 5.4 记忆层（memory 持久化落地）

- `store.py` 重构为 `Protocol`（MemoryStore）+ `InMemoryStore`（测试）+ `RedisMemoryStore`（生产）：键 `mem:user:{id}` / `mem:trip:{id}`，带 TTL，行程记忆 `LTRIM` 保留最近 `MAX_TRIP_MEMORIES=10` 条
- `manager.py`：否定前缀拆分（`_NEGATION_PREFIXES`，长前缀优先），否定偏好进 `disliked_styles`；"不要 X"写回布尔 False（`accept_theme_park` 等）
- `schema.py`：`TripMemory` 新增 `created_at`、`feedback` 改 `response_mode`；各字段补语义注释
- `MemoryManager` 支持 store 注入（测试用 InMemoryStore 不依赖 Redis）

### 5.5 编排与上下文

- orchestrator：
  - **空泛改稿预检**：新增 `_REVISION_SIGNAL_KEYWORDS` + `_EMPTY_REVISE_PATTERN`，"改一下"这类无具体指令的改稿不再直接执行，改为追问"想调整哪里"
  - 规划/改稿成功后写产物标记 `has_plan` / `plan_updated_at`；产物丢失时清空摘要，避免 revise 分支"反复提示已过期"死循环
  - 新增 `_commit_artifacts_with_retry`：`save_with_artifacts` 冲突时以最新会话为基底重放产物标记 + stage + assistant 消息（最多 3 次）
- planning：删除 `"[改稿模式]"` 摘要前缀 hack；行程记忆落库 `feedback` 改 `response_mode`
- context：`builder` 的 confirmed 字段从 days/budget 对齐为 `start_date` / `end_date`；`ResponseContext` 删除无消费字段（`needs_follow_up` / `audience_notes` 等），`response_mode` 用 Literal 收紧

### 5.6 验证

- 新增 `test/test_memory.py`（115 行）：否定偏好提取与防御（"不要紧凑"不进正向）、布尔翻转（"不要乐园"→False）、矛盾时正向优先、存储融合往返、匿名跳过、行程记忆上限裁剪、`response_mode` 语义

### 涉及文件（36 个）

新增：`domain/common/{chat,dates}.py`、`test/test_memory.py`
迁移：`domain/intent_type.py` → `domain/common/intent_type.py`、`domain/stage.py` → `domain/common/stage.py`、`domain/session_context.py` → `domain/common/session_context.py`
修改：`.gitignore`、`README.md`、`agents/{orchestrator,planning}.py`、`domain/context/{builder,planning,response,session,user}.py`、`domain/intent/{adapter,prompt,schema,service}.py`、`domain/memory/{manager,schema,store}.py`、`domain/session/{mapper,merge,pipeline,repository,schema,service}.py`
删除：`agents/prompt/orchestrator.py`、`agents/sparse/orchestrator.py`、`travel_agent.egg-info/*`（5 个）、`阶段.md`

---

## 6. `f4629a4` context/session/memory/intent 的再优化（2026-08-15 19:41）

32 个文件、+674 / -427。第二次优化：跨层模型统一归位 `domain/common/`（agents / infrastructure 层瘦身为 re-export）、并发保存返回值升级、fallback 一次解析、偏好清洗与历史行程记忆、**列表字段追加语义**。

### 6.1 跨层共享模型统一（单一来源 + re-export）

- 新增 `common/planning.py`：`TravelRequestFields`（intent/session 共享字段基类，字段单一来源）、`PlanningRequest`、`TripPlan`、`REQUIRED_PATCH_FIELDS`、`compute_missing_fields`（三处共用）、`extract_plan_attractions`（只记最终采纳景点）
- 新增 `common/itinerary.py`：`ItineraryDraftSchema` 等行程稿模型（从 `infrastructure/llm/schemas.py` 迁出，`ItineraryDayPlan.validate_block_mix` 校验随迁）
- 新增 `common/reflection.py`：`ReflectionIssue` / `ReflectionResult` / `ReflectionLLMResult`
- 新增 `common/time.py`：`utc_now` 统一时钟（各层不再散落 `datetime.now(timezone.utc)`）
- `context/user.py` → `common/user.py`（`UserContext`）；`SessionContext` 并入 `common/session_context.py`（+ `compute_confirmed_fields`），删除 `context/session.py`
- `context/builder.py`：`confirmed_fields` 改用 `compute_confirmed_fields(request)`（与 intent/session 判定同一函数）；planning_agent 收到的 `conversation_stage` 固定为 `"ready_to_plan"`（修正原先恒为 `collecting_destination` 的误导值，planning 本就只在字段齐后进入）
- `context/planning.py`、`agents/revise.py`：import 全部改引 common 定义（PlanningContext 不再引用已删除的 context/session.py）
- `agents/schema/planning.py`、`reflection.py`、`revise.py`、`infrastructure/llm/schemas.py` 瘦身为 re-export + 保留本层专属模型（planning 层保留 `PlanInput`/`PlanLoopInput`/`PlannerStatus`/`DialogueDecision` 等，删除 `TripPlan`/`PlanningRequest`/`PlanUserContext`/`PlanSessionContext`/`PlanAgentInput`/`PlanAgentOutput`/`PlanArtifacts`/`PlanDebugTrace`/`PlanExecutionPolicy` 等 9 个迁出定义）
- `agents/planning.py`：`persist_trip_memory` 的景点清单从 `attraction_result.candidates`（候选全集）改为 `extract_plan_attractions(state.plan)`（**只记最终采纳进行程的景点**），行程记忆更贴合实际成行内容

### 6.2 并发保存返回值升级（消除二次保存误判）

- `save` / `save_with_artifacts` / `_optimistic_save` 从 bool 改为返回**升版后的 `SessionState | None`**
- 调用方用升版 state 作为后续保存基底，避免"磁盘已 +1、内存仍是旧版本"导致同请求内第二次保存被误判冲突

### 6.3 `_resolve_intent` 重试升级

- **意图上下文指纹**：重建 intent 输入做值比较，上下文变化（并发请求刚完成规划）才重跑识别，未变则复用首次识别结果只重放 merge+apply——避免重复调 LLM
- 空输入防护下移到 `_resolve_intent`（不调 LLM 直接 unknown 兜底，与 pipeline 口径一致）
- 每次迭代重建 `user_context` / `trip_history`（重试时并发请求可能已改偏好，旧值会让意图判定失真）

### 6.4 意图层

- `IntentRecognitionInput` 增加 `trip_history`；`IntentPlanningRequest` 改继承 `TravelRequestFields`（不再重复定义字段）
- prompt 侧展示裁剪：`recent_messages` 只取最近 4 条、`trip_history` 只取最近 5 条（避免把全量历史喂给 LLM 放大 token 成本）
- fallback **一次性解析**：日期/人数/目的地只解析一次，confirm/qa/unknown 判定通过 `parsed` 复用，避免同一消息被多条分支重复正则扫描

### 6.5 会话层

- `SessionRequestState` 从继承 `IntentPlanningRequest` 改为继承 `TravelRequestFields`（与 intent 解耦，可自由扩展会话专属字段）
- **列表字段改为"去重追加合并"**（解决待办 P2-1）：intent 层只输出本轮新增项，`preferences` 等不再整体替换——先"喜欢美食"后"轻松一点"不再互相覆盖
- 删除 `ReviseSessionContext` / `ReviseUserContext` 及两个转换 mapper（`session_context_to_revise_session_context` / `user_context_to_revise_user_context`），revise 直接用共享 `SessionContext` / `UserContext`（pace 口径统一 relaxed/dense，不再 slow/fast 映射）
- `agents/schema/repair.py`：`RepairUserContext.pace_preference` 同步从 `slow/balanced/fast` 改为 `relaxed/dense`（与共享 UserContext 口径一致）
- `agents/schema/revise.py`：`ReviseAgentInput` 增加 `trip_history` 字段（历史行程摘要，供改稿意图理解参考）
- 状态机：`qa/reject` 在 `revise_collecting` / `revise_ready` / `ready_to_plan` 等"就绪执行"阶段也写回 qa（避免用户在改稿收集/待规划间隙闲聊被路由到执行分支）
- mapper：travelers 列表文案 `"adults"` → `"位成人"`；时间统一 `utc_now`，同请求内取一次 now 保证各时间戳一致

### 6.6 记忆层

- 偏好清洗 `_normalize_preference`：去常见前后缀 + 中缀噪声（"想轻松一点"→"轻松"）；"不错"不误判为否定
- `MAX_PREFERENCE_ITEMS=20` 容量上限（超限淘汰最早条目）；存量记忆读取时**回溯清洗 + 惰性回写**（仅当清洗结果有变化才落盘，幂等）
- 否定信号语义调整：本轮"不要 X"可覆盖历史正向（偏好可翻转），本轮正向最高优先（矛盾时正向胜）
- `load_trip_history`：按目的地去重聚合成轻量历史（供 intent / revise 参考）
- `RedisMemoryStore`：`load_trip_memories` 读到内容即续期 TTL（防活跃用户行程记忆静默过期）、坏数据 warning 日志

### 6.7 编排与改稿

- revise 分支补齐记忆沉淀：改稿消息中的新偏好并入长期记忆（`persist_user_memory`）、改稿落行程记忆（`persist_trip_memory`，`response_mode="revise_plan"`）——之前只有规划分支会沉淀
- `revise_input` 直接用共享 `UserContext` / `SessionContext`；向 revise 传递 `trip_history`（`build_revision_intent_prompt` 增加历史行程段落，供"已去过哪/避坑"参考）

### 6.8 验证

- `test/test_memory.py` 扩增：偏好清洗归一（含中缀噪声）、存量脏数据回溯清洗、容量上限、行程记忆 TTL 续期（fake redis 注入验证）

### 涉及文件（32 个）

新增：`domain/common/{itinerary,planning,reflection,time}.py`
迁移：`domain/context/user.py` → `domain/common/user.py`
修改：`agents/{orchestrator,planning,revise}.py`、`agents/schema/{planning,reflection,repair,revise}.py`、`agents/sparse/revise.py`、`domain/common/session_context.py`、`domain/context/{builder,planning}.py`、`domain/intent/{adapter,prompt,schema,service}.py`、`domain/memory/{manager,schema,store}.py`、`domain/session/{mapper,merge,pipeline,repository,schema,service}.py`、`infrastructure/llm/schemas.py`、`test/test_memory.py`
删除：`domain/context/session.py`

---

## 7. `0bd8861` domain/infrastructure/observability 当前文件审查（2026-08-16 17:10）

39 个文件、+157 / -262。以文件审查与清理为主：包结构扁平化、LLM/高德客户端瘦身、外部调用容错、意图边界修正。

### 7.1 包结构扁平化

- `infrastructure/config/settings.py` → `infrastructure/settings.py`（删 `config/__init__.py`）
- `infrastructure/llm/client.py` → `infrastructure/llm_client.py`（删 `llm/__init__.py`、`llm/schemas.py`）
- `infrastructure/amap/client.py` → `infrastructure/amap_client.py`（删 `amap/__init__.py`）
- `budgets/tracker.py` → `observability/token_budget.py`；删 `budgets/policy.py`——`TokenBudgetPolicy` 并入 `token_budget.py`，精简为 `total_budget` 单字段（其余意图/规划/改稿等分项预算无消费方，属死配置）
- `RepairDayPlanSchema` / `RepairProposalSchema` 从 `infrastructure/llm/schemas.py` 迁到 `agents/schema/repair.py`（repair 专属模型归位 agents 层）
- 全部 import 同步更新

### 7.2 LLM 客户端

- `last_debug_info` 改为 `threading.local`（线程安全，不再跨请求污染调试信息）
- `AuthenticationError` 认证失败直接终止（重试无意义）；API 错误线性退避重试（`0.5 * (attempt+1)` 秒）

### 7.3 高德客户端精简

- 删除未使用方法：`get_poi_by_id` / `measure_distance` / `plan_bicycling`
- `plan_transit` / `plan_walking` 删除 `raw` / `best` / `steps` 大字段（瘦身响应）；`plan_driving` 增加出租车费用（route 级 `taxi_cost`）
- `get_weather_forecast` 固定 `extensions="all"`，去掉参数

### 7.4 外部调用容错

- `TransportTool`：`_fetch_routes` / `_resolve_poi` 加 try/except——高德不可用/报错时降级为无路线/None（由 `_query_one` 置 error 提示），不阻断主链路

### 7.5 意图边界修正（`domain/intent/schema.py`）

- `_clean_patch` 成对日期**不再清 days**：显式 days 由 session merge 按"patch 显式给 days 时以 days 为准重算区间"处理，避免用户显式天数（如"8月10到12号，玩5天"）被区间推导静默吞掉
- `_is_clean_destination_name` 增加 "改"/"换" 残留信号过滤（"不去了，改成上海"不再把脏值当确认目的地，宁可追问）

### 7.6 记忆 / 编排细节

- `_PREFERENCE_MIDDLE_NOISE` 顺序修正：长短语优先（"的地方玩"先于"的地方"匹配），"想去轻松一点的地方玩"→"轻松"（修复上一版残留"轻松玩"）
- 改稿偏好持久化改为基于 merge 后的完整 request 重建 `user_context`（布尔/否定偏好如"不要乐园"→`accept_theme_park=False` 与规划分支走同一逻辑）
- 各层 docstring 精简（审查清理，无行为变化）

### 7.7 验证

- `test/test_intent.py`：新增改口句目的地提取用例（"不去了，改成上海" / "我要去成都改成北京" → None）
- `test/test_memory.py`：适配新的偏好归一结果（"想去轻松一点的地方玩"→"轻松"）

### 涉及文件（39 个）

迁移（含内容变化）：`infrastructure/config/settings.py` → `infrastructure/settings.py`、`infrastructure/llm/client.py` → `infrastructure/llm_client.py`、`infrastructure/amap/client.py` → `infrastructure/amap_client.py`、`budgets/tracker.py` → `observability/token_budget.py`
修改：`agents/{orchestrator,planning,reflection,repair,revise}.py`、`agents/schema/{orchestrator,repair,revise}.py`、`agents/sparse/revise.py`、`domain/common/{chat,dates,intent_type,planning,session_context}.py`、`domain/context/builder.py`、`domain/intent/{schema,service}.py`、`domain/memory/{manager,store}.py`、`domain/session/{merge,repository,schema,service}.py`、`infrastructure/redis_client.py`、`tools/{attraction,lodging,transport,weather}.py`、`test/{test_intent,test_memory}.py`
删除：`budgets/policy.py`、`infrastructure/{amap/__init__,config/__init__,llm/__init__,llm/schemas}.py`

---

## 8. `f2099dd` weather tool 修改（2026-08-17 10:48）

13 个文件、+334 / -262。三件事：天气数据源从高德切换为和风、lodging 工具规则化第一轮（去 LLM）、高德 POI 搜索字段补全（为 lodging 提供评分/星级/电话等证据）。

### 8.1 天气数据源切换（高德 → 和风）

- `infrastructure/settings.py`：新增和风配置——`qweather_api_key`（预报接口专用 key）、`qweather_geo_api_key`（城市搜索接口专用 key，二者独立不通用）、`qweather_host`（项目自定义域名）、`qweather_forecast_days`（订阅最大预报天数）、`qweather_timeout_seconds`
- `.env.example` / `README.md`：补充 QWEATHER_* 环境变量说明，环境要求、分层职责中的"高德天气"改为"和风天气"
- `tools/weather.py` 重写（数据流：城市搜索 → LocationID/经纬度 → 每日预报）：
  - `_search_city`：和风城市搜索（`geo/v2/city/lookup`，仅返回预报需要的 location_id / lng / lat）
  - `_fetch_forecast`：优先用 LocationID，无则用"经度,纬度"（最多两位小数）调用 `get_daily_forecast`
  - `_forecast_days_needed`：所需预报天数 = 行程结束日距今天 + 1（和风预报从今天起算）
  - `_parse_date` 复用 `normalize_date`（兼容 `YYYY-MM-DD` / `2026/8/17` / `8月17日` 等格式）
  - `_build_daily` 按行程日期逐天对齐：真实预报日期匹配，缺失的天补空占位（保证 daily 与行程天数一一对应），返回 `(daily, covered)`
  - 预报窗口未覆盖行程日期时返回明确错误提示（和风仅支持未来 N 天预报）
  - WeatherDay 字段扩展：`weather` → `weather_day`，新增 `wind`（风向+风力级）、`humidity`、`precip`
- `tools/schema/weather.py`：`WeatherResult` 新增 `coverage_start` / `coverage_end`
- agents 侧天气字段适配：planning / reflection / repair 中 `item.weather` → `weather_day`，repair 当日天气提取改为先取 day 对象再取值
- 新增 `test/tool/test_weather.py`：手动测试脚本（fixture 自动补全 `.env` 中的 QWEATHER 配置，`test_weather_run` 可改城市/日期范围后运行观察输出）

### 8.2 高德 POI 字段补全（lodging 前置准备）

- `amap_client.search_pois`：`/v5/place/text` → `/v3/place/text`（v5 同参数不返回评分/星级/电话等字段）
- normalized 输出增加：`tel`、`business_area`、`keytag`、`price`（biz_ext 最低价或成本）、`rating`（biz_ext）、`star`（从 POI type 提取星级）
- 新增 `_extract_star`（"四星级酒店" → `"4"`）与 `_safe_str`（空值归一为 None）

### 8.3 lodging 工具规则化第一轮（去 LLM）

- 删除 LLM 依赖：`get_llm_client` / `LODGING_TOOL_PROMPT` / `_llm_json` / `_context` / `_query_prompt` / `_selection_prompt` / `_parse_queries` / `_parse_selection` / `_select_candidates` / `_fallback_select`；删除 `tools/prompt/lodging.py`
- 新增 `_grade_words` 档位词：preferences 里的档位偏好（星/经济型/连锁/舒适/豪华/高档/快捷）优先，否则按 budget 推导（≤300 经济型 / ≤600 舒适型 / ≤1500 四星 / 更高五星）
- 新增 `_build_queries` 规则生成搜索词（优先级 base > spot > grade > pref，带 query_type 标记）
- `_score_candidate` 评分增强：rating（评分×2）、star（星级越高越优）、near_spot（来自景点附近搜索加权）
- `_filter_candidates`：avoid_spots 命中候选名即硬排除
- `_choose_selected_lodging`：无强住宿词候选时改从全部候选中选取
- `schema/lodging.py`：`LodgingCandidate` 增加 near_spot / price / rating / star / lng / lat / address / tel；`LodgingInput` 各字段补充语义注释（budget 驱动档位词、avoid_spots 命中即剔除）

### 涉及文件（13 个）

修改：`.env.example`、`README.md`、`agents/{planning,reflection,repair}.py`、`infrastructure/{amap_client,settings}.py`、`tools/{lodging,weather}.py`、`tools/schema/{lodging,weather}.py`
新增：`test/tool/test_weather.py`
删除：`tools/prompt/lodging.py`

---

## 9. `120fdf9` 新增和风天气客户端（2026-08-17 10:48）

1 个文件（新增）、+104。与 `f2099dd` 同批的配套提交，补齐天气模块依赖的 `qweather_client`。

- 新增 `infrastructure/qweather_client.py`：
  - `QWeatherClientConfig`（dataclass，slots）：`api_key` / `geo_api_key` / `host` / `timeout_seconds` / `forecast_days`，`from_env` 从 settings 装载
  - `QWeatherClient`：
    - 新版认证：`X-QW-Api-Key` 请求头
    - `get_daily_forecast`：按所需天数自动向上取整选择 `3d/7d/10d/15d/30d` 端点，且不超过订阅上限 `QWEATHER_FORECAST_DAYS`；响应归一为 date / weather_day / temp_max / temp_min / wind_dir_day / wind_scale_day / humidity / precip
    - `_pick_endpoint`：在订阅上限内把所需天数向上取整到最近的可用预报端点
    - `is_enabled`：要求 api_key + host 均配置
  - 全局单例 `qweather_client`

### 涉及文件（1 个）

新增：`infrastructure/qweather_client.py`

---

## 10. `6c8e690` lodging tool 优化完成（2026-08-17 16:27，当前 HEAD）

8 个文件、+510 / -264。lodging 工具第二轮全面重构：纯规则检索 + 距离/价格综合评分 + 输出契约精简（只把必要基本信息交给 Agent）。

### 10.1 检索与排序核心重构

- 新增 `_RankedCandidate`（dataclass）：候选 + 经纬度 + 最近/平均景点距离 + 是否来自景点附近查询 + 综合分
- run 管线：`_normalize_input`（去重/strip，不修改入参）→ `_build_queries` → `_spot_coords` → `_search_candidates` → `_filter_and_rank` → top 5
  - `_normalize_input`：对 destination / preferences / avoid_keywords / spots 去重清洗，入参对象本身不被改动
  - `_spot_coords`：对最多 4 个行程景点调高德 geocode 取坐标
  - `_search_candidates`：`city_limit=True` 精确限定城市；按 poi_id 或"名称+地址"跨查询去重合并（同一酒店多次命中只保留一份，景点标记取并集）
  - `_filter_and_rank`：avoid_keywords 命中即排除 → 最近景点 > 40km 排除 → 缺经纬度或身份信息（poi_id/address）排除 → 综合评分排序
- `_score_candidate` 评分体系重写：
  - 名称强住宿词（酒店/饭店/宾馆/hotel）加权
  - 预算价格比（price/budget）分档：≤1 减 4 / ≤1.15 加 1 / ≤1.35 加 6 / 超 1.35 加 14；有预算但无价格信息加 2
  - near_spot 查询减 2；最近景点距离（≤3km 减 5 / ≤8km 减 2 / >15km 加 7 / >25km 加 18）；平均景点距离（≤8km 减 4 / >15km 加 4 / >25km 加 9）
  - preferences 命中加权
- 新增 `_haversine_km`（球面距离）、`_is_non_lodging`（噪声词/非住宿词白名单过滤）、`_candidate_key`（去重键）
- 删除旧逻辑：`_merge_transport_spots`、`_derive_area_hints`、`_is_strong_lodging_name`、`_is_noise_spot`、`_is_non_lodging_poi`、`_filter_candidates`、`_choose_selected_lodging`、`_summary`

### 10.2 输出契约精简（schema 瘦身）

- `LodgingCandidate`：精简为 poi_id / name / area / price / address / tel（仅提供给 Agent 的基本信息），删除 source / tags / near_spot / rating / star / lng / lat
- `LodgingInput`：`avoid_spots` → `avoid_keywords`
- 删除 `SelectedLodging`；`LodgingResult` 删除 selected_lodging / source / raw（只保留 city / candidates / summary）
- `PlanningContext.selected_lodging` 类型由 `SelectedLodging` 改为 `LodgingCandidate`
- planning / revise 侧：`avoid_spots=["民宿","旅社招待所"]` → `avoid_keywords=["招待所"]`；`selected_lodging` 改取 `candidates[0]`
- `sparse/planning.py`：`_summarize_lodging_candidates` 只输出 poi_id/name/area/price/address/tel 六个字段 + instruction（提示不虚构候选外酒店），删除 `_lodging_warning`

### 10.3 配套

- `pyproject.toml`：新增 `[tool.pytest.ini_options]` markers（`integration`：调用真实外部服务的测试）
- 新增 `test/tool/test_lodging.py`（237 行）：
  - 7 个单元测试：输出仅含候选基本信息字段、预算+多景点距离排序、非住宿与 avoid 关键词过滤、缺坐标/身份过滤、跨查询去重、run 不修改入参、候选上限 5 个
  - 5 个集成测试（`@pytest.mark.integration`，真实调高德）：北京高预算（五星/亲子）、广州经济型（多景点）、成都四星（熊猫基地）、杭州默认（西湖）、上海商务（外滩+东方明珠）

### 涉及文件（8 个）

修改：`agents/{planning,revise}.py`、`agents/sparse/planning.py`、`domain/context/planning.py`、`tools/lodging.py`、`tools/schema/lodging.py`、`pyproject.toml`
新增：`test/tool/test_lodging.py`

---

## 附：当前待办（已记录未实施）

- ~~**P2-1 列表字段追加语义**~~：已解决（`f4629a4`）——`merge.py` 列表字段改为"去重追加合并"，用户说"也喜欢自然"会追加到"人文"而非覆盖
- **P2-2 closed 会话拦截**：`end_session` 置 closed 后，后续消息仍会走完整链路（甚至重新规划），需考虑 closed 后拒绝响应或自动续开
