weather agent后期优化
1. 缓存天气结果
同一个 city + date_range，同一个请求天数，同一个城市短时间内重复查询
降低外部接口调用 降低 LLM 重复解释 明显减少延时
2. 增加容错与降级
天气接口可能失败，LLM 也可能失败。
天气接口失败时，返回空天气结果，但保留 city
LLM 失败时，直接用天气原始信息生成一个最小建议集
如果解析失败，返回默认安全策略，例如“优先室内、预留雨具”


plan agent
plan agent 用来规划旅游行程，由llm通过接收输入，自主调用四个子agent获得信息，
并综合信息填写到一个旅游行程上，不断审查这个旅游行程进行编排，
如果缺失或者优化，可以自主再次调用子agent，不断优化，直到llm觉得这个旅游行程可行。
-需要通过语义知道时间范围或者直接上游得到，从而服务天气agent查询
-需要对lodging输入景点，用户偏好，交通便捷等输入，可以看lodging agent输入
-对于attration需要输入地点、天数、必去景点和不去景点，候选景点
-对于transport只需要输入地点，可以多地点或者两个地点


我来完整说明整个attraction agent：目的就是筛选景点，筛选出高质量景点让规划agent去编排流程。
首先，根据输入（地点、天数、必去景点和不去景点）对必去和不去景点进行校验，并进行普通poi筛选。
获得一系列景点过后，通过必去和不去景点，进行筛选出静待你，并一次性给出duration和理由，并进行审查。
注意：
景点校验可能会干扰普通景点query 的poi查询；
筛选景点一定要审查筛选景点可能存在包含关系；
prompt一定不能有对特定场景进行约束，很像打补丁，不能指标不治本；
选出的静待你数量，可以让llm根据天数去自主决定，llm还需要考虑上游agent进行旅行编排时有些景点其实并不可用，所以不能仅仅满足天数塞满就行，还需要尽可能多一点
给出duration和景点理由；
能够让上游agent很好接收



当前链路现在是
必去 / 不去验证
LLM 生成 query
搜索普通候选
去噪 / 去重 / 去 avoid
LLM 补充 reason / duration
LLM 最终推荐
LLM 再判断候选之间的依赖关系，删掉子景区/局部点


后期需要考虑的是，日期修改，如果是延后一天应该怎么处理，延后一周又是如何处理，这设计到重新规划一天和重新规划整个行程两个方向







